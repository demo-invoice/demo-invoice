/** localStorage key for the active (in-progress) invoice. */
export const INVOICE_ACTIVE_KEY = 'invoice_active_v1';

/** localStorage key for the saved invoice history array. */
export const INVOICE_HISTORY_KEY = 'invoice_history_v1';
