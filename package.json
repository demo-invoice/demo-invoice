{
  "name": "demo-invoice",
  "version": "1.0.0",
  "description": "Demo invoice application",
  "scripts": {
    "test": "vitest run",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "vite": "^5.0.0"
  }
}
